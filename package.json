{
  "name": "twitter-monitor",
  "private": true,
  "scripts": {
    "build": "echo 'No build step needed - Netlify Functions only'",
    "dev": "netlify dev"
  },
  "dependencies": {
    "twitter-api-v2": "^1.15.1",
    "@netlify/functions": "^2.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^15.0.0"
  }
}